# Test: explicitly request federated bundle
validation_context_sds_secret_config:
  name: "spiffe://cluster-b"  # Try explicit trust domain
  sds_config:
    resource_api_version: V3
    api_config_source:
      api_type: GRPC
      transport_api_version: V3
      grpc_services:
        - envoy_grpc:
            cluster_name: sds-grpc
